name: VLCKit macOS x64 cross

on:
  push:
  pull_request:

jobs:
  VLCKit_macOS_x64_cross:
    if: github.event_name == 'push' || github.event.pull_request.head.repo.id != github.event.pull_request.base.repo.id
    runs-on: macos-14
    env:
      VLCKIT_TAG: 3.6.1b1
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      
      - name: VLCKit macOS x64 cross build
        shell: bash
        run: |
          # Clone VLCKit repository
          git clone https://github.com/videolan/vlckit
          cd vlckit
          git checkout "tags/$VLCKIT_TAG"
          
          # Build VLCKit for macOS x64 cross
          ./buildMobileVLCKit.sh -x -a x86_64 -v
          
          # Create output directory
          mkdir -p $GITHUB_WORKSPACE/build/macos-x64-cross
          
          # Copy built framework
          cp -R build/VLCKit.xcframework $GITHUB_WORKSPACE/build/macos-x64-cross/
      
      - name: Upload VLCKit macOS x64 cross build
        uses: actions/upload-artifact@v4
        with:
          name: vlckit-macos-x64-cross-build
          path: build/macos-x64-cross/